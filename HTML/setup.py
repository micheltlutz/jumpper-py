from setuptools import setup, find_packages

setup(
    name="HTML",
    version="0.1",
    packages=find_packages(),
    install_requires=[
        # Aqui você pode listar quaisquer dependências que seu pacote possa ter, 
        # por exemplo: 'requests', 'numpy', etc.
    ]
)